# Study Tracker (minimal)

This is a minimal static study tracker that lets you add planned sessions and mark them done. It stores data in `localStorage`.

How to use:
1. Open `index.html` in your browser.
2. Add a topic and duration, click "Add Session".
3. Click "Done" to remove a session (mark complete).

Next steps: export to CSV, add date fields, calendar integration.